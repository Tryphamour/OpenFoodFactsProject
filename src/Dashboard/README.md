# Dashboard Context

Personal dashboard composition and widget layout lifecycle.

- `Domain`: dashboard and widget invariants.
- `Application`: widget commands and dashboard queries.
- `Infrastructure`: persistence adapters.
- `UI`: live components, controllers, and templates orchestration.

