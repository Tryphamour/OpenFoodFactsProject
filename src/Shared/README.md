# Shared

Cross-cutting abstractions used by bounded contexts.

- `Domain`: framework-agnostic domain contracts and primitives.
- `Application`: orchestration-level interfaces used by use cases.
- `Infrastructure`: technical implementations for shared contracts.
- `UI`: shared presentation helpers.

